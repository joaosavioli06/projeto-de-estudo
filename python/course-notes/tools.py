# Aquivo criado para se utilizar junto com o arquivo "modulos-e-pip.py"

PI = 3.14
GRAVITY = 9.8

# toda variável em maiúsculo é como se fosse uma "constante". Na prática não é 100% constante, mas é como se fosse


def get_extension(file):
    return file[file.index(".") +1:]

def highest_number(numbers):
    return max(numbers)

